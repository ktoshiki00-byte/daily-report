# 日報ボット 運用マニュアル

日々の使い方と、スタッフの追加・削除の手順をまとめています。
セットアップ手順（初回構築）は [README.md](README.md) を参照してください。

---

## スタッフ向け

### 日報の入力

1. 平日18時に「今日の日報入力」のお知らせが届きます
2. **仕事が終わったら**、**「日報を入力」** を押してフォームを開きます
3. 午前・午後の行動種別を選び、内容を入力して **「送信する」**
4. 「✅ 記録しました」と表示されれば完了です

- **18時に入力しなくて構いません。** お知らせは目安です。
  残業などでまだ仕事中の場合は、**終わってから**入力してください
- お知らせを消してしまっても、**「日報入力」** と送信すればフォームを開けます
- 午前だけ・午後だけの日は、ない方を **「なし」** にしてください
- 午前・午後の両方を「なし」にすると送信できません
- **初回はプロフィールへのアクセス許可** を求められます。「許可する」を押してください

### ⏰ 締切：当日24時まで

- **その日の日報は、当日の24時まで**に入力してください
- 24時を過ぎると **翌日の日報** として記録されます
- **前の日にさかのぼって入力することはできません**。入れ忘れた分は上長に直接ご連絡ください
- 19時30分の時点で未入力だと、管理者に届くまとめの「19:30時点で未入力」に
  名前が載ります。その後24時までに入力すれば、日報はその日の分として記録されます

### 残業した日は

**終わった時間に入力してください。** 18時に間に合わなくても問題ありません。
例えば21時まで残業した日は、21時に入力すればその日の日報として記録されます。

入れ忘れて日付が変わってしまった場合は、さかのぼって入力する方法はありません。
上長に直接ご連絡ください。

### 送信に失敗したとき

「⚠️ 保存に失敗しました。もう一度送信してください。」と出た場合は、
**入力内容はそのまま残っています**。もう一度「送信する」を押してください。
繰り返し失敗する場合は管理者にご連絡ください。

### その他のコマンド

| 送信する言葉 | できること |
|---|---|
| `日報入力` | 入力フォームを開く |
| `週次まとめ` | 今週の自分の日報を確認（金曜19:30に自動でも届きます） |
| `確認` | 日報の照会（1日分・月別・期間指定）。**自分の分だけ**表示されます |
| `使い方` | 使い方ガイドを表示 |
| `休暇申請` / `有給残日数` / `申請履歴` | 休暇・有給の手続き |

### 休暇の申請

`休暇申請` と送り、種別（有給／欠勤／山休み／その他）・期間・理由を入力します。
申請すると管理者に通知が届き、**承認または却下されるとLINEに結果が届きます**。

- 有給が承認されると「7/25の有給が承認されました。残り9日です。」のように届きます
- 却下された場合は担当者にご確認ください
- 承認されるまで残日数は減りません

上記以外のメッセージを送ると、内容に応じて自動案内が返るか、管理者に転送されます。

---

## 管理者向け

### 自動で届くもの

| タイミング | 内容 | 宛先 |
|---|---|---|
| 平日 18:00 | 日報入力のお知らせ | スタッフ全員 |
| 平日 19:30 | 入力状況まとめ（入力済みの人と内容・19:30時点で未入力の人） | 管理者 |
| 金曜 19:30 | 週次まとめ（全員分の提出率・主な活動） | 管理者 |
| 金曜 19:30 | 週次まとめ（本人の分） | スタッフ各自 |

19:30のまとめは **締切前の集計** です。「19:30時点で未入力」に名前が出ても
未提出が確定したわけではありません。残業などで入力が遅くなることもあるため、
24時までに入力されれば、日報はその日の分として記録されます。

### 日報の照会

`確認` と送信すると、1日分・月別・期間指定で照会できます。
管理者の場合は「全員」または個人を選べます（スタッフは自分の分のみ）。

### ✅ 新しいスタッフを追加する

**管理者の作業は不要です。** 本人に次の3つをやってもらってください。

1. 日報ボットを **友だち追加** する
2. **「登録する」** を押す（または `登録` と送信）
3. 「○○さんを登録しました！」が返れば完了

これで `users` シートに **LINE表示名・ユーザーID・登録日** が自動で記録され、
翌営業日から18時のお知らせが届くようになります。

> **友だち追加しただけでは登録されません。** 「登録する」を押す必要があります。
> 追加したのにお知らせが届かない場合、まずここを確認してください。

### ❌ 退職者を通知対象から外す

`users` シートから **その人の行を削除** してください。それだけで、

- 18時のお知らせが届かなくなります
- 19:30のまとめの「19:30時点で未入力」に出なくなります
- 週次まとめの対象から外れます

> ⚠️ **行そのものを削除してください。**
> 行番号を右クリック →「行を削除」。
> セルの**内容だけ**を消して空行を残す方法でも動きますが、
> 見た目が分かりにくくなるため行ごと削除するのが確実です。

過去の日報は「日報」シートに残ります（照会も可能です）。消したい場合は
「日報」シート側の行を削除してください。

### 名前を変えたスタッフがいる場合

日報は **LINEの表示名** で本人と紐づけています。表記ゆれ（大文字小文字・
全角半角・前後の空白）は自動で吸収しますが、**表示名そのものを変更**すると
（例：`eguchi` → `江口`）過去の日報と紐づかなくなります。

その場合は `users` シートの **LINE表示名** を新しい表示名に書き換えてください。
過去分も揃えたい場合は「日報」シートの `ユーザー名` 列を一括置換します。

### スプレッドシートの構成

| シート | 内容 |
|---|---|
| `日報` | 日報本体（1行 = 午前 or 午後の1件） |
| `users` | 登録スタッフ（LINE表示名・ユーザーID・登録日） |
| `管理者` | 管理者のLINE_USER_ID・名前 |
| `休暇申請` | 申請の記録（ステータス・承認者・承認日時） |
| `有給管理` | 有給の付与・使用・残日数 |
| `問い合わせ` | 自動転送した問い合わせ |

> ⚠️ **シートの途中に空行を作らないでください。** 空行があると集計や通知が
> 正しく動かない場合があります。行を消すときは「行を削除」を使ってください。

### 🏖 休暇申請の承認・却下

スタッフが休暇申請すると、管理者のLINEに通知が届きます。

```
休暇申請が届きました

申請者：eguchi
種別：有給
期間：7/25〜7/27（3日）
理由：私用

現在の残日数：1日
⚠️ 申請日数（3日）が残日数を超えています
```

**［承認］［却下］** のボタンを押すと処理されます。

- **承認**：申請シートにステータス・承認者・日時が記録され、**有給の場合のみ**
  残日数が申請日数分減ります。本人にLINEで結果が届きます
- **却下**：残日数は減りません。本人にLINEで結果が届きます
- 残日数が足りなくても **承認は可能** です（管理者判断）。通知に警告が出ます
- 同じ申請に2回押しても **二重には処理されません**（「すでに承認済みです」と返ります）
- 有給以外（欠勤・山休み・その他）は残日数に影響しません

> ボタンは通知に付いています。通知を消してしまった場合は、
> 「休暇申請」シートの該当行の**ステータスを直接書き換える**こともできますが、
> その場合は残日数の減算と本人への通知が行われません。ボタンの使用を推奨します。

### 有給残日数の管理

`有給一覧` と送ると（管理者のみ）、全員の残日数を確認できます。

```
🏖 有給残日数の一覧

・ eguchi: 残 10日（付与 12 / 使用 2）
❓ 山田: 不明

❌ 有給管理シートに未登録
　・新人

【要対応】
　・山田: 残日数・付与日数・入社日がいずれも未設定
```

#### 初期設定・修正のしかた

**「有給管理」シートに直接入力してください。** 1行1名です。

| 列 | 内容 | 必須 |
|---|---|---|
| 名前 | LINEの表示名と一致させる | ✅ |
| LINE_USER_ID | `users` シートからコピー | |
| 入社日 | 例 `2024/04/01` | △ |
| 付与日数 | 例 `12` | △ |
| 使用日数 | 例 `2` | |
| 残日数 | 例 `10` ← **この値が使われます** | ✅ |

- **残日数がこのシステムの正**です。承認時に「残日数 − 申請日数」で引かれます
- **繰越分がある場合は、繰越を含めた日数を残日数に入れてください**
- 残日数が空欄の場合のみ「付与日数 − 使用日数」で補います。付与日数も空欄なら
  入社日から自動計算します（勤続6ヶ月で10日、以降1年半11日…最大20日）
- 残日数を直接書き換えれば、その値がそのまま使われます（上書きされません）

> 名前が `users` シートやLINEの表示名とずれていると残日数を更新できません。
> 承認時に「⚠️ 残日数は更新できませんでした」と返る場合はここを確認してください。

### 管理者を追加する

`管理者` シートに **LINE_USER_ID** と **名前** の行を追加してください。
ユーザーIDは `users` シートで本人の行から確認できます。

---

## 困ったとき

| 症状 | 確認すること |
|---|---|
| 18時のお知らせが届かない | `users` シートにその人の行があるか。無ければ本人に「登録」してもらう |
| フォームが開かない | LINEアプリから開いているか。ブラウザのリンクを直接開くと動きません |
| 「保存に失敗しました」が続く | 管理者に連絡。Renderのログに原因（`日報の保存に失敗`）が出ています |
| 日報を出したのに未入力の扱いになる | `users` シートの表示名と、本人の現在のLINE表示名が違う可能性（上記「名前を変えたスタッフ」参照） |
| 退職者に通知が届き続ける | `users` シートの行が残っている。行ごと削除する |
